version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/Feple_BE
permissions:
  - object: /home/ubuntu/Feple_BE/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu